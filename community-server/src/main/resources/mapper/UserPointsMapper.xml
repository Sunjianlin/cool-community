<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cool.server.mapper.UserPointsMapper">

    <select id="selectByUserId" resultType="com.cool.pojo.entity.UserPoints">
        SELECT * FROM user_points WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.cool.pojo.entity.UserPoints">
        INSERT INTO user_points (user_id, points, version, update_at)
        VALUES (#{userId}, #{points}, #{version}, NOW())
    </insert>

    <update id="addPoints">
        UPDATE user_points 
        SET points = points + #{points}, version = version + 1, update_at = NOW()
        WHERE user_id = #{userId} AND version = #{version}
    </update>

    <update id="reducePoints">
        UPDATE user_points 
        SET points = points - #{points}, version = version + 1, update_at = NOW()
        WHERE user_id = #{userId} AND version = #{version} AND points >= #{points}
    </update>

    <insert id="insertPointsTransaction" parameterType="com.cool.pojo.entity.PointsTransaction">
        INSERT INTO points_transaction (user_id, points, type, create_at)
        VALUES (#{userId}, #{points}, #{type}, NOW())
    </insert>

</mapper>
