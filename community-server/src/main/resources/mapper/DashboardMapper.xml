<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cool.server.mapper.DashboardMapper">

    <select id="countUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user WHERE deleted = 0
    </select>

    <select id="countPosts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM post WHERE deleted = 0
    </select>

    <select id="countTopics" resultType="java.lang.Long">
        SELECT COUNT(*) FROM topic WHERE deleted = 0
    </select>

    <select id="countProducts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM product WHERE deleted = 0
    </select>

    <select id="countTodayPosts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM post WHERE deleted = 0 AND DATE(create_time) = CURDATE()
    </select>

    <select id="countTodayUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user WHERE deleted = 0 AND DATE(create_time) = CURDATE()
    </select>

</mapper>
