<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cool.server.mapper.ChatMessageMapper">

    <!-- 插入消息 -->
    <insert id="insert" parameterType="com.cool.pojo.entity.ChatMessage">
        INSERT INTO chat_message (
            session_id, sender_id, receiver_id, content, 
            type, is_read, status, create_time, update_time
        ) VALUES (
            #{sessionId}, #{senderId}, #{receiverId}, #{content}, 
            #{type}, #{isRead}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 更新消息 -->
    <update id="update" parameterType="com.cool.pojo.entity.ChatMessage">
        UPDATE chat_message SET
            content = #{content},
            type = #{type},
            is_read = #{isRead},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 根据ID查询消息 -->
    <select id="selectById" parameterType="java.lang.Long" resultType="com.cool.pojo.entity.ChatMessage">
        SELECT * FROM chat_message WHERE id = #{id}
    </select>

    <!-- 根据会话ID查询消息列表 -->
    <select id="selectBySessionId" parameterType="map" resultType="com.cool.pojo.entity.ChatMessage">
        SELECT * FROM chat_message 
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 查询用户的未读消息 -->
    <select id="selectUnreadByReceiverId" parameterType="java.lang.Long" resultType="com.cool.pojo.entity.ChatMessage">
        SELECT * FROM chat_message 
        WHERE receiver_id = #{receiverId} AND is_read = 0
        ORDER BY create_time DESC
    </select>

    <!-- 标记消息为已读 -->
    <update id="markAsRead" parameterType="map">
        UPDATE chat_message SET
            is_read = 1,
            update_time = NOW()
        WHERE session_id = #{sessionId} AND receiver_id = #{receiverId} AND is_read = 0
    </update>

    <!-- 统计会话的消息数 -->
    <select id="countBySessionId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM chat_message WHERE session_id = #{sessionId}
    </select>

    <!-- 统计用户的未读消息数 -->
    <select id="countUnreadByReceiverId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM chat_message WHERE receiver_id = #{receiverId} AND is_read = 0
    </select>

    <!-- 删除会话的所有消息 -->
    <delete id="deleteBySessionId" parameterType="java.lang.Long">
        DELETE FROM chat_message WHERE session_id = #{sessionId}
    </delete>

    <!-- 删除消息 -->
    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM chat_message WHERE id = #{id}
    </delete>

</mapper>