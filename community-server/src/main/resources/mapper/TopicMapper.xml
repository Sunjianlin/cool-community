<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cool.server.mapper.TopicMapper">

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM topic
        <where>
            <if test="true">
                deleted = 0
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="list" resultType="com.cool.pojo.vo.TopicVO">
        SELECT id, name, description, icon, cover, category, follow_count, post_count, is_hot
        FROM topic
        <where>
            <if test="true">
                deleted = 0
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY follow_count DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="listHot" resultType="com.cool.pojo.vo.TopicVO">
        SELECT id, name, description, icon, cover, category, follow_count, post_count, is_hot
        FROM topic
        <where>
            <if test="true">
                is_hot = 1 AND deleted = 0
            </if>
        </where>
        ORDER BY follow_count DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="getDetailById" resultType="com.cool.pojo.vo.TopicVO">
        SELECT id, name, description, icon, cover, category, follow_count, post_count, is_hot
        FROM topic WHERE id = #{id} AND deleted = 0
    </select>

</mapper>
