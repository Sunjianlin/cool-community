<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cool.server.mapper.UserSeckillRecordMapper">
    
    <insert id="insert" parameterType="com.cool.pojo.entity.UserSeckillRecord">
        INSERT INTO user_seckill_record (
            user_id, activity_id, status, create_time
        ) VALUES (
            #{userId}, #{activityId}, #{status}, #{createTime}
        )
    </insert>
    
    <select id="countByUserIdAndActivityId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user_seckill_record 
        WHERE user_id = #{userId} AND activity_id = #{activityId}
    </select>
</mapper>
